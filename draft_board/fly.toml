app = "draft-board"
primary_region = "ord"

[build]
  dockerfile = "Dockerfile"

[env]
  MIX_ENV = "prod"
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1